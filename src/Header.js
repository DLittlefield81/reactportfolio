import React from 'react'
import { AppBar, Typography } from '@mui/material'
const Header = () => {
    return (
        <React.Fragment>
            <AppBar>
                <Typography>

                    Dennis Littlefield Profile
                </Typography>
            </AppBar>
        </React.Fragment>
    )
}

export default Header